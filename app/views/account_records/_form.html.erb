<%= form_with(model: account_record) do |form| %>
  <% if account_record.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(account_record.errors.count, "error") %> prohibited this account_record from being saved:</h2>

      <ul>
        <% account_record.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :amount, style: "display: block" %>
    <%= form.text_field :amount %>
  </div>

  <div>
    <%= form.label :author_id, style: "display: block" %>
    <%= form.text_field :author_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
